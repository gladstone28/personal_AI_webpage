// Adding interactivity to "Read More" buttons
document.querySelectorAll('.read-more').forEach(button => {
    button.addEventListener('click', () => {
        alert("More content will be added soon. Stay tuned!");
    });
});
